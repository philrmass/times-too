import React from 'react';
import styles from '../styles/GameControls.module.css';

function GameControls() {
  return (
    <div className={styles.main}>GameControls</div>
  );
}

export default GameControls;
